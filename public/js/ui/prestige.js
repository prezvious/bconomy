// Prestige Panel Renderer (Delegates to Combined Rank & Ascension Renderer)
import { renderRankPrestige } from './rankPrestigeCombined.js';

export const renderPrestige = () => {
    renderRankPrestige();
};
